# 04. Action Plan

Decisions, next steps, and the Kanban task list for this project. Every task reference is a
clickable `[[id-slug|ID]]` wikilink per `~/.claude/rules/kanban-card-linking.md`. never a bare
card ID.

**`great-plan.md`** lives here. the hardened phase-by-phase plan from the `jj-master-plan`
loop, produced at scaffold time, including the `## Parallel vs Sequential Execution` section.
Start from it. If it's missing, run `/master-plan` against this project before doing real work.

## Kanban sync

This folder is the project-side mirror of whatever card(s) on JJ's Kanban boards spawned this
project. See the project `CLAUDE.md` § "Kanban sync" for the read/write contract that keeps this
folder, `TASK.md`, and the board in sync. this file doesn't restate it.
